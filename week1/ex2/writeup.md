## Terminal calculator

In order to access the script's parameters, we need `sys.argv`.
Then, we can get the input values by looping through the array.
Finally, just switch based on the operation.
If an **invalid** one or a **division by zero** is performed, return `"Error!"`.
